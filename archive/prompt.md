# Original prompt (voice transcript)

Captured via speech-to-text on 2026-07-24 and processed into `spec/starter.md`
and `context/index.md` per the spec-starter pattern. Verbatim:

> not sure if something like this exists, but if it doesn't, can we create
> it? I'm looking for a GUI for Ubuntu, um, and, basically, a image editor
> who's sole purpose is obfuscation, reduction, censorship, protection, that
> kind of thing. find a great utility codes for QR code detection. Runs
> locally in Python works beautifully. I'd like to integrate that. Um,
> there's also, like... it'll be also nice to have a way to use, uh, Google
> Cloud Vision. Um, and like a tool kit, let's say that I wanted to add...
> open a folder or photograph in the app, like, a function we're scanning,
> it's saying QR codes detected in these images. other classes that are
> exposed in that app at the computer and locally. And then manual things
> like cropping and a couple of blurs that can be added. So, basically, a...
> and then, of course, like, you know, saving maybe a review function in
> which you can open up a folder, um, first and then work through an actual,
> like, review checklist, like, programmatic review, manual review compared
> before and after. We don't need to build it all in one day, but let's,
> like, have a start... set up the public repository, create a a spec, and
> then we can iterate from there.
