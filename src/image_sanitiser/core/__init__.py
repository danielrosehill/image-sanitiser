"""Pure image-processing core: models, redaction primitives, verification.

Nothing in this package may import Qt — the core must stay usable from a
future headless CLI (`image-sanitiser scan DIR`).
"""
